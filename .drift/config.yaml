version: 1
provider:
  name: mock
  model: test-model

storage:
  type: json-file

thresholds:
  regression: 0.10
  alert: 0.20

baseline:
  source: branch

concurrency: 5
timeoutMs: 30000

suite: fixtures/example-suite.yaml
