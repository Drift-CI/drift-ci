{
  "name": "@drift-ci/action",
  "version": "1.1.0",
  "private": true,
  "description": "drift-ci GitHub Action — native Node 20 entrypoint bundled by ncc",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-bundle.mjs",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rimraf dist build"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^9.1.1",
    "@drift-ci/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "@vercel/ncc": "^0.44.0",
    "@vitest/coverage-v8": "^4.1.8",
    "rimraf": "^6.0.0",
    "typescript": "^6.0.3",
    "vite": "^7.3.5",
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
